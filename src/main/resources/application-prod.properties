# Production Profile Configuration
# This profile uses MongoDB Atlas cloud cluster

# Disable embedded MongoDB for production
spring.data.mongodb.embedded.enabled=false

# MongoDB Atlas Configuration
spring.data.mongodb.uri=mongodb+srv://forchatgpt3:PracticeAdda@cluster0.ucapo0p.mongodb.net/schoolmanagement?retryWrites=true&w=majority&appName=Cluster0
spring.data.mongodb.database=ai_school_management
spring.data.mongodb.auto-index-creation=true

# Enhanced connection settings for MongoDB Atlas
spring.data.mongodb.connect-timeout=30000
spring.data.mongodb.socket-timeout=30000
spring.data.mongodb.server-selection-timeout=30000

# Disable debugging for production
app.mongodb.debug.enabled=false
app.mongodb.cleanup.enabled=false

# Production Server Configuration
server.port=8080
server.servlet.context-path=/api

# Production Logging (reduced verbosity)
logging.level.com.eduai.schoolmanagement=INFO
logging.level.org.springframework.security=WARN
logging.level.org.springframework.data.mongodb=WARN
logging.level.de.flapdoodle.embed.mongo=OFF

# Production Email Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:forchatgpt3@gmail.com}
spring.mail.password=${MAIL_PASSWORD:Bablu@123}
spring.mail.from=${MAIL_FROM:noreply@eduai-school.com}
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.auth=true

# Production-specific settings
spring.jpa.show-sql=false
spring.data.mongodb.repositories.enabled=true

# Active profile indication
spring.config.activate.on-profile=prod
